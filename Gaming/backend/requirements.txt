# Core
opencv-python
numpy
mss
pyautogui
pynput
pyyaml


# High-Performance Capture (60fps+)
# Install ONE of these for GPU-accelerated capture:
dxcam            # Recommended: fastest DXGI capture (~120fps+)

# OCR for Story Mode
easyocr          # GPU-accelerated OCR (recommended)
# pytesseract    # Alternative OCR (requires Tesseract binary)

# Controller Support
pygame-ce        # Gamepad/joystick detection and input reading

# NVIDIA GPU Monitoring
nvidia-ml-py     # NVIDIA Management Library for GPU metrics

# NVIDIA Optimization (optional)
# torch           # PyTorch with CUDA (install via pytorch.org for your CUDA version)


# vgamepad        # Virtual gamepad for AI agent controller output

# Voice Assistant (STT/TTS)
pyttsx3          # Text-to-Speech
SpeechRecognition # Speech-to-Text
pyaudio          # Required by SpeechRecognition for microphone input

# System/Hardware Check
psutil           # RAM/CPU monitoring
WMI              # Windows hardware names/info
pywin32          # Required for WMI
orjson           # Ultra-fast JSON
pydantic         # High-speed Data Models
packaging        # Version parsing
openai           # NVIDIA NIM / OpenAI API Client
requests         # HTTP Requests for voice/game updates
python-dotenv    # Environment variable management
websockets>=16.0

# Production Database (Supabase / PostgreSQL)
psycopg2-binary>=2.9.12  # PostgreSQL adapter (binary wheel, no local Postgres required)

# LangChain RAG & Vector Database
langchain
langchain-community
langchain-nvidia-ai-endpoints
langchain-text-splitters
faiss-cpu
rank_bm25
tiktoken
mem0ai
